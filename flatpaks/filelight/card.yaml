version: 1

flatpak:
  id: org.anatase.DiskUsageAnalyzer
  command: filelight
  rename: Disk Usage Analyzer (Filelight)
  rename-desktop-file: org.kde.filelight.desktop
  rename-appdata-file: org.kde.filelight.appdata.xml
  rename-icon: filelight
  finish-args: |-
    --device=dri
    --filesystem=host:ro
    --share=ipc
    --socket=fallback-x11
    --socket=wayland
    --filesystem=xdg-config/kdeglobals:ro
    --talk-name=org.kde.kconfig.notify
    --talk-name=org.kde.KGlobalSettings

build-deps:
  - rpm-build
  - rpmdevtools
  - redhat-rpm-config
  - flatpak-rpm-macros
  - ccache

specs:
  - spec: git+https://src.fedoraproject.org/rpms/filelight:filelight.spec#branch=f$releasever
    packages:
      - filelight
