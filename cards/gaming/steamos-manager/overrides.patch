From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Sat, 8 Nov 2025 18:11:08 +0100
Subject: [NA]: Update readme + CI

---
 .gitignore    |   1 +
 Containerfile |  19 +++++++
 README.md     | 143 +-------------------------------------------------
 sync.sh       |  26 +++++++++
 4 files changed, 47 insertions(+), 142 deletions(-)
 create mode 100644 Containerfile
 create mode 100755 sync.sh

diff --git a/.gitignore b/.gitignore
index 4e8c2af2b9c5..ab6af6b986bd 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,2 +1,3 @@
+vendor
 /target
 /tarpaulin-report.html
diff --git a/Containerfile b/Containerfile
new file mode 100644
index 000000000000..538e5abf408c
--- /dev/null
+++ b/Containerfile
@@ -0,0 +1,19 @@
+ARG FEDORA_VERSION=42
+
+FROM fedora:${FEDORA_VERSION:-43}
+
+RUN dnf install -y mock mock-core-configs spectool rust2rpm git
+
+ARG UID=1000
+ARG GID=1000
+
+RUN groupadd -g $GID -o builder && \
+    useradd -m -u $UID -g $GID -G mock -o -s /bin/bash builder && \
+    echo "builder ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/builder && \
+    chmod 0440 /etc/sudoers.d/builder
+
+USER builder
+
+WORKDIR /workspace
+
+ENTRYPOINT [ "env" ]
\ No newline at end of file
diff --git a/README.md b/README.md
index 1f892b76f1f2..5d866b62ac02 100644
--- a/README.md
+++ b/README.md
@@ -1,144 +1,3 @@
 # SteamOS Manager
 
-SteamOS Manager is a system daemon that aims to abstract Steam's interactions
-with the operating system. The goal is to have a standardized interface so that
-SteamOS specific features in the Steam client, e.g. TDP management, can be
-exposed in any Linux distro that provides an implementation of this DBus API.
-
-The interface may be fully or partially implemented. The Steam client will
-check which which features are available at startup and restrict the settings
-it presents to the user based on feature availability.
-
-Some of the features that SteamOS Manager enables include:
-- GPU clock management
-- TDP management
-- BIOS/Dock updates
-- Storage device maintenance tasks
-- External storage device formatting
-  - Steam generally performs device enumeration via UDisks2, but formatting
-	happens via SteamOSManager
-
-For a full list of features please refer to the [interface specification](https://gitlab.steamos.cloud/holo/steamos-manager/-/blob/main/data/interfaces/com.steampowered.SteamOSManager1.xml).
-
-Other notable dbus interfaces used by the Steam Client include:
-- org.freedesktop.UDisks2
-- org.freedesktop.portal.desktop
-- org.freedesktop.login1
-- org.bluez
-
-# Building
-
-This project is written in Rust, so you will need an implementation for the
-device you're using for building. The [Arch wiki
-article](https://wiki.archlinux.org/title/rust) has some good guidelines for
-this, but this mostly consists of installing `rustup` from the
-`rustup` package (if you're on Arch) and running `rustup default stable` to get
-an initial toolchain, or just installing the regular `rust` package for a
-system-managed installation.
-
-Once you have that and `cargo` is in your path, to build the project you can
-use `cargo build`.
-
-# Developing
-
-As far as IDEs go, Visual Studio Code works pretty well for giving errors about
-things you are changing and has plugins for vim mode, etc. if you are used to
-those keybindings. Most/all IDEs that work with language servers should do that
-fine though.
-
-For VS Code, these extensions help: `rust` and `rust-analyzer`.
-
-Before committing code, please run `cargo fmt` to make sure that your code
-matches the preferred code style, and `cargo clippy` can help with common
-mistakes and idiomatic code.
-
-To perform tests, run `cargo test`. This will compile a test configuration of
-the project and run the built-in test suite.
-
-## Interface compatibility notes
-
-SteamOS Manager and the Steam client are normally updated independently of each
-other, thus the interface must remain binary compatible across releases.
-
-In general, when making changes to the interface please consider the following:
-- Method signatures must not be altered
-  - Instead, prefer exposing a new symbol and add a compatibility adapter to
-	the previous interface
-- Changes in behaviour should be avoided
-  - Consider how a change would affect the beta and stable release of the Steam
-	client
-- Features must have a mechanism to discover if they are available or not
-  - E.g. for a feature exposed as a property if the property is not present on
-	the bus it means the feature is unsupported.
-
-Note that while SteamOS Manager must never break binary compatibility of
-the interface, the Steam client makes no guarantees that older versions of
-an interface will be used if available. As a rule of thumb, the client will
-always provide full support for the SteamOS Manager interface version available
-in the Stable release of SteamOS.
-
-Another pitfall is that while most of the properties do signal when they are
-changed by SteamOS Manager itself, several of these properties can also change
-out from under SteamOS Manager if something on the system bypasses it. While
-this should never be the case if the user doesn't prod at the underlying system
-manually, it's something that interface users should be aware of.
-
-## Implementation details
-
-SteamOS Manager is compromised of two daemons: one runs as the logged in user
-and exposes a public DBus API on the session bus, and the second daemon runs as
-the `root` user. The root daemon exposes a limited DBus API on the system bus
-for tasks that require elevated permissions to execute.
-
-The DBus API exposed on the system bus is considered a private implementation
-detail of SteamOS Manager and it may be changed at any moment and without
-warning. For this reason, we don't provide an XML schema for the system
-daemon's interface and clients shouldn't use it directly.
-
-## Extending the API
-
-To extend the API with a new method or property update the XML schema and
-extend the user daemon's DBus API, which is implemented in
-`src/manager/user.rs`. Make sure to also update the proxy implementation in
-`src/proxy.rs` and expose it in steamosctl, in `src/bin/steamosctl.rs`.
-
-If the new functionality requires elevated privileges, instead extend the
-system daemon's DBus API in `src/manager/root.rs` with the necessary helpers to
-complete the task. However, you should keep as much logic as possible in the
-user daemon.
-
-# Interoperability
-
-While SteamOS Manager aims to support as broad of a range of hardware
-internally as possible, sometimes external tools support things better. To that
-end, external daemon an register their own remote implementation of various
-interfaces that SteamOS Manager will relay DBus traffic to and from. At the
-current time a remote implementation cannot override a local implementation;
-they can only fill in holes where a local implementation does not exist.
-
-To register a remote interface, the daemon must place a TOML file in either
-`/usr/share/steamos-manager/remotes.d` or `/etc/steamos-manager/remotes.d`
-specifying information about the remote. The TOML file should contain sections
-named based on the interface it implements, along with a `bus_name` key
-specifying the well-known bus name of the remote and an `object_path` key
-specifying the path on that remote to the object that implements the interface.
-Currently the remote must be on the system bus, not the session bus.
-
-New remote configurations cannot be added or removed at runtime, but existing
-remotes configured as above will be dynamically detected at runtime and added or
-removed as appropriate.
-
-The following interfaces can be implemented remotely. Others will be added in
-the future.
-
-- BatteryChargeLimit1
-- CpuBoost1
-- FactoryReset1
-- FanControl1
-- GpuPerformanceLevel1
-- GpuPowerProfile1
-- PerformanceProfile1
-- Storage1
-- TdpLimit1
-- UpdateBios1
-- UpdateDock1
+Simple fork of [Steamos-manager](https://gitlab.steamos.cloud/holo/steamos-manager) that allows stubbing tdp calls with Handheld Daemon, plus some other minor fixups.
diff --git a/sync.sh b/sync.sh
new file mode 100755
index 000000000000..b24cdda3eb84
--- /dev/null
+++ b/sync.sh
@@ -0,0 +1,26 @@
+if [ -z "$1" ]; then
+    echo "Usage: $0 <host>"
+    exit 1
+fi
+
+set -e
+
+HOST=$1
+RSYNC="rsync -rv --exclude .git --exclude venv --exclude __pycache__'"
+USER=${2:-bazzite}
+
+cargo build -p steamos-manager
+$RSYNC target/debug/steamos-manager $HOST:
+
+ssh $HOST /bin/bash << EOF
+    sudo rpm-ostree usroverlay --hotfix
+    sudo mv steamos-manager /usr/bin/steamos-manager
+    sudo chmod +x /usr/bin/steamos-manager
+    sudo chcon -R -u system_u -r object_r --type=bin_t /usr/bin/steamos-manager
+
+    sudo systemctl stop steamos-manager.service
+    systemctl --user stop steamos-manager.service
+    sudo systemctl start steamos-manager.service
+    systemctl --user start steamos-manager.service
+    bazzite-session-select gamescope
+EOF
\ No newline at end of file

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Sat, 8 Nov 2025 19:05:02 +0100
Subject: feat: add support for running concurrently with Handheld Daemon

Add passthrough for TDP limit to handheld daemon. Then, if Handheld
Daemon is active, disable CPU/GPU/battery conservation features in
SteamOS Manager to avoid conflicts. If Handheld Daemon is inactive,
SteamOS Manager will manage power features as before.

In addition, re-use the conflict detection of Decky plugins from
handheld daemon to disable power features if they are detected.
---
 steamos-manager/src/gpu.rs          | 142 +++++++++++++++++++++++++++-
 steamos-manager/src/manager/user.rs |  68 ++++++++-----
 steamos-manager/src/power.rs        | 132 ++++++++++++++++++++++++--
 3 files changed, 305 insertions(+), 37 deletions(-)

diff --git a/steamos-manager/src/gpu.rs b/steamos-manager/src/gpu.rs
index a828071f60bd..03e39b7966fa 100644
--- a/steamos-manager/src/gpu.rs
+++ b/steamos-manager/src/gpu.rs
@@ -15,13 +15,15 @@ use std::ops::RangeInclusive;
 use std::path::{Path, PathBuf};
 use std::str::FromStr;
 use std::sync::LazyLock;
+use std::sync::atomic::{AtomicBool, Ordering};
 use strum::{Display, EnumString, VariantNames};
 use tokio::fs::{self, File, try_exists};
 use tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader};
+use tokio::process::Command;
 use tracing::{debug, error};
 
 use crate::hardware::{device_config, device_type};
-use crate::power::find_hwmon;
+use crate::power::{HHD_CMD, HhdStatus, find_hwmon};
 use crate::{path, write_synced};
 
 pub(crate) const AMDGPU_HWMON_NAME: &str = "amdgpu";
@@ -150,7 +152,145 @@ pub(crate) async fn gpu_power_profile_driver() -> Result<Box<dyn GpuPowerProfile
     })
 }
 
+async fn get_hhd_status() -> HhdStatus {
+    let output = Command::new(HHD_CMD)
+        .arg("steamos-gpu")
+        .arg("get")
+        .output()
+        .await;
+
+    if output.is_err() {
+        return HhdStatus::Inactive;
+    }
+
+    let code = output.unwrap().status.code().unwrap_or(0);
+    match code {
+        1 => HhdStatus::Inactive,
+        2 => HhdStatus::Conflicts,
+        _ => HhdStatus::Active,
+    }
+}
+
+#[derive(Debug)]
+pub(crate) struct HhdPerformanceLevelDriver {
+    manual: AtomicBool,
+}
+
+impl AmdgpuGpuPerfDriver for HhdPerformanceLevelDriver {}
+
+#[async_trait]
+impl GpuPerformanceLevelDriver for HhdPerformanceLevelDriver {
+    fn performance_level_from_str(&self, value: &str) -> Result<GpuPerformanceLevel> {
+        Ok(GpuPerformanceLevel::Intel(IntelPerformanceLevel::from_str(
+            value,
+        )?))
+    }
+
+    async fn get_available_performance_levels(&self) -> Result<Vec<GpuPerformanceLevel>> {
+        Ok(vec![
+            GpuPerformanceLevel::Intel(IntelPerformanceLevel::Auto),
+            GpuPerformanceLevel::Intel(IntelPerformanceLevel::Manual),
+        ])
+    }
+
+    async fn get_performance_level(&self) -> Result<GpuPerformanceLevel> {
+        // Just lie so that we always get a trigger to auto
+        Ok(GpuPerformanceLevel::Intel(IntelPerformanceLevel::Manual))
+    }
+
+    async fn get_clocks(&self) -> Result<u32> {
+        // Dummy value
+        Ok(1200)
+    }
+
+    async fn set_performance_level(&self, level: GpuPerformanceLevel) -> Result<()> {
+        let manual = level == GpuPerformanceLevel::Intel(IntelPerformanceLevel::Manual);
+        self.manual.store(manual, Ordering::Relaxed);
+
+        if manual {
+            return Ok(());
+        }
+        let output = Command::new(HHD_CMD)
+            .arg("steamos-gpu")
+            .arg("clear")
+            .output()
+            .await
+            .map_err(|e| anyhow!("Failed to clear gpu limit: {e}"))?;
+
+        ensure!(
+            output.status.success(),
+            "hhd.steamos steamos-tdp exited with status {status}",
+            status = output.status
+        );
+
+        Ok(())
+    }
+
+    async fn get_clocks_range(&self) -> Result<RangeInclusive<u32>> {
+        let output = Command::new(HHD_CMD)
+            .arg("steamos-gpu")
+            .arg("get")
+            .output()
+            .await
+            .map_err(|e| anyhow!("Failed to execute hhd.steamos steamos-tdp get: {e}"))?;
+
+        ensure!(
+            output.status.success(),
+            "hhd.steamos get exited with status {status}",
+            status = output.status
+        );
+
+        let stdout = String::from_utf8(output.stdout)
+            .map_err(|e| anyhow!("Invalid UTF-8 from hhd.steamos steamos-tdp get: {e}"))?;
+
+        let mut parts = stdout.split_whitespace();
+        let min: u32 = parts
+            .next()
+            .ok_or_else(|| anyhow!("Missing min value"))?
+            .parse()
+            .map_err(|e| anyhow!("Failed to parse min value: {e}"))?;
+        let max: u32 = parts
+            .next()
+            .ok_or_else(|| anyhow!("Missing max value"))?
+            .parse()
+            .map_err(|e| anyhow!("Failed to parse max value: {e}"))?;
+        Ok(min..=max)
+    }
+
+    async fn set_clocks(&self, clocks: u32) -> Result<()> {
+        if !self.manual.load(Ordering::Relaxed) {
+            return Ok(());
+        }
+
+        let output = Command::new(HHD_CMD)
+            .arg("steamos-gpu")
+            .arg(clocks.to_string())
+            .output()
+            .await
+            .map_err(|e| anyhow!("Failed to execute hhd.steamos steamos-tdp {clocks}: {e}"))?;
+
+        ensure!(
+            output.status.success(),
+            "hhd.steamos steamos-tdp exited with status {status}",
+            status = output.status
+        );
+
+        Ok(())
+    }
+}
+
 pub(crate) async fn gpu_performance_level_driver() -> Result<Box<dyn GpuPerformanceLevelDriver>> {
+    let hhd_status = get_hhd_status().await;
+
+    if hhd_status == HhdStatus::Active {
+        debug!("Using handheld daemon for TDP limiting");
+        return Ok(Box::new(HhdPerformanceLevelDriver {
+            manual: AtomicBool::new(false),
+        }));
+    } else if hhd_status == HhdStatus::Conflicts {
+        bail!("Conflicting GPU controls found");
+    }
+
     let config = device_config().await?;
     let config = config
         .as_ref()
diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index 083389fdc7c9..4f1f4b83466c 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -49,10 +49,7 @@ use crate::manager::{MANAGER_PATH, RemoteInterface, RemoteInterfaceConfig, Remot
 use crate::path;
 use crate::platform::platform_config;
 use crate::power::{
-    BATTERY_DEFAULT_SUGGESTED_MINIMUM_LIMIT, CpuSchedulerManager, TdpManagerCommand,
-    get_available_cpu_scaling_governors, get_available_platform_profiles, get_cpu_boost_state,
-    get_cpu_scaling_governor, get_max_charge_level, get_platform_profile, register_tdp_limit1,
-    unregister_tdp_limit1,
+    BATTERY_DEFAULT_SUGGESTED_MINIMUM_LIMIT, CpuSchedulerManager, TdpManagerCommand, enable_power_features, get_available_cpu_scaling_governors, get_available_platform_profiles, get_cpu_boost_state, get_cpu_scaling_governor, get_max_charge_level, get_platform_profile, register_tdp_limit1, unregister_tdp_limit1
 };
 use crate::proxy::{
     BatteryChargeLimit1Proxy, CpuBoost1Proxy, FactoryReset1Proxy, FanControl1Proxy,
@@ -1737,11 +1734,8 @@ async fn create_device_interfaces(
     proxy: &Proxy<'static>,
     object_server: &ObjectServer,
     tdp_manager: Option<UnboundedSender<TdpManagerCommand>>,
+    power_features: bool,
 ) -> Result<()> {
-    let Some(config) = device_config().await? else {
-        return Ok(());
-    };
-
     let performance_profile = PerformanceProfile1 {
         proxy: proxy.clone(),
         tdp_limit_manager: tdp_manager.clone(),
@@ -1751,14 +1745,8 @@ async fn create_device_interfaces(
         let low_power_mode = LowPowerMode1 {
             manager: manager.clone(),
         };
-        if config
-            .tdp_limit
-            .as_ref()
-            .and_then(|config| config.download_mode_limit)
-            .is_some()
-        {
-            object_server.at(MANAGER_PATH, low_power_mode).await?;
-        }
+
+        object_server.at(MANAGER_PATH, low_power_mode).await?;
 
         let object_server = object_server.clone();
         spawn(async move {
@@ -1775,6 +1763,14 @@ async fn create_device_interfaces(
         });
     }
 
+    if !power_features {
+        return Ok(());
+    }
+
+    let Some(config) = device_config().await? else {
+        return Ok(());
+    };
+
     if let Some(config) = config.performance_profile.as_ref()
         && !get_available_platform_profiles(&config.platform_profile_name)
             .await
@@ -1868,7 +1864,9 @@ pub(crate) async fn create_interfaces(
 
     let object_server = session.object_server();
 
-    if let Err(e) = create_device_interfaces(&proxy, object_server, tdp_manager.clone()).await {
+    let power_features = enable_power_features().await;
+
+    if let Err(e) = create_device_interfaces(&proxy, object_server, tdp_manager.clone(), power_features).await {
         error!("Failed to initalize device-specific interfaces: {e}");
     }
 
@@ -1891,17 +1889,35 @@ pub(crate) async fn create_interfaces(
         object_server.at(MANAGER_PATH, wifi_debug_dump).await?;
     }
 
-    if get_max_charge_level().await.is_ok() {
-        object_server.at(MANAGER_PATH, battery_charge_limit).await?;
-    }
+    if power_features {
+        if get_max_charge_level().await.is_ok() {
+            object_server.at(MANAGER_PATH, battery_charge_limit).await?;
+        }
 
-    if get_cpu_boost_state().await.is_ok() {
-        object_server.at(MANAGER_PATH, cpu_boost).await?;
-    }
+        if get_cpu_boost_state().await.is_ok() {
+            object_server.at(MANAGER_PATH, cpu_boost).await?;
+        }
 
-    object_server.at(MANAGER_PATH, cpu_scaling).await?;
-    if CpuSchedulerManager::is_supported().await? {
-        object_server.at(MANAGER_PATH, cpu_scheduler).await?;
+        object_server.at(MANAGER_PATH, cpu_scaling).await?;
+        if CpuSchedulerManager::is_supported().await? {
+            object_server.at(MANAGER_PATH, cpu_scheduler).await?;
+        }
+
+        match gpu_power_profile_driver().await {
+            Ok(driver) => {
+                object_server
+                    .at(
+                        MANAGER_PATH,
+                        GpuPowerProfile1 {
+                            proxy: proxy.clone(),
+                            driver,
+                            order: SerialOrderValidator::default(),
+                        },
+                    )
+                    .await?;
+            }
+            Err(e) => warn!("Can't add GpuPowerProfile1 interface: {e}"),
+        }
     }
 
     match gpu_performance_level_driver().await {
diff --git a/steamos-manager/src/power.rs b/steamos-manager/src/power.rs
index 52554283ee48..168fa6c19faa 100644
--- a/steamos-manager/src/power.rs
+++ b/steamos-manager/src/power.rs
@@ -10,6 +10,7 @@ use async_trait::async_trait;
 use nix::errno::Errno;
 use num_enum::TryFromPrimitive;
 use serde::Deserialize;
+use tokio::process::Command;
 use std::collections::HashMap;
 use std::collections::hash_map::Entry;
 use std::num::NonZeroU32;
@@ -24,7 +25,7 @@ use tokio::net::unix::pipe;
 use tokio::sync::mpsc::{UnboundedReceiver, UnboundedSender};
 use tokio::sync::oneshot;
 use tokio::task::JoinSet;
-use tracing::{debug, error, warn};
+use tracing::{debug, error, info, warn};
 use zbus::names::OwnedBusName;
 use zbus::zvariant::OwnedObjectPath;
 use zbus::{Connection, ObjectServer, fdo};
@@ -149,6 +150,8 @@ pub(crate) struct RemoteInterfaceLimitManager<'proxy> {
     proxy: Option<TdpLimit1Proxy<'proxy>>,
 }
 
+pub(crate) struct HhdTdpManager {}
+
 #[async_trait]
 pub(crate) trait TdpLimitManager: Send + Sync {
     async fn get_tdp_limit(&self) -> Result<u32>;
@@ -169,6 +172,15 @@ pub(crate) trait TdpLimitManager: Send + Sync {
 }
 
 pub(crate) async fn tdp_limit_manager(system: &Connection) -> Result<Box<dyn TdpLimitManager>> {
+    let hhd_status = get_hhd_status().await;
+
+    if hhd_status == HhdStatus::Active {
+        debug!("Using handheld daemon for TDP limiting");
+        return Ok(Box::new(HhdTdpManager {}));
+    } else if hhd_status == HhdStatus::Conflicts {
+        bail!("Conflicting TDP limiting method found");
+    }
+
     let config = device_config().await?;
     if let Some(config) = config.as_ref().and_then(|config| config.tdp_limit.as_ref()) {
         Ok(match &config.method {
@@ -661,6 +673,102 @@ impl<'proxy> TdpLimitManager for RemoteInterfaceLimitManager<'proxy> {
     }
 }
 
+
+#[derive(PartialEq, Debug)]
+pub(crate) enum HhdStatus {
+    Inactive,
+    Active,
+    Conflicts,
+}
+
+pub(crate) const HHD_CMD: &str = "hhd.steamos"; // "/home/bazzite/hhd-dev/hhd/venv/bin/hhd.steamos";
+
+async fn get_hhd_status() -> HhdStatus {
+    let output = Command::new(HHD_CMD)
+        .arg("steamos-tdp")
+        .arg("get")
+        .output()
+        .await;
+
+    if output.is_err() {
+        return HhdStatus::Inactive;
+    }
+
+    let code = output.unwrap().status.code().unwrap_or(0);
+    match code {
+        1 => HhdStatus::Inactive,
+        2 => HhdStatus::Conflicts,
+        _ => HhdStatus::Active,
+    }
+}
+
+pub(crate) async fn enable_power_features() -> bool {
+    let hhd_status = get_hhd_status().await;
+    info!("Handheld daemon TDP status: {:?}", hhd_status);
+    hhd_status == HhdStatus::Inactive
+}
+
+impl HhdTdpManager {}
+
+#[async_trait]
+impl TdpLimitManager for HhdTdpManager {
+    async fn get_tdp_limit(&self) -> Result<u32> {
+        anyhow::bail!("Getting TDP from handheld daemon is not implemented.");
+    }
+
+    async fn set_tdp_limit(&self, limit: u32) -> Result<()> {
+        let output = Command::new(HHD_CMD)
+            .arg("steamos-tdp")
+            .arg(limit.to_string())
+            .output()
+            .await
+            .map_err(|e| anyhow!("Failed to execute hhd.steamos steamos-tdp {limit}: {e}"))?;
+
+        ensure!(
+            output.status.success(),
+            "hhd.steamos steamos-tdp exited with status {status}",
+            status = output.status
+        );
+
+        Ok(())
+    }
+
+    async fn get_tdp_limit_range(&self) -> Result<RangeInclusive<u32>> {
+        let output = Command::new(HHD_CMD)
+            .arg("steamos-tdp")
+            .arg("get")
+            .output()
+            .await
+            .map_err(|e| anyhow!("Failed to execute hhd.steamos steamos-tdp get: {e}"))?;
+
+        ensure!(
+            output.status.success(),
+            "hhd.steamos steamos-tdp get exited with status {status}",
+            status = output.status
+        );
+
+        let stdout = String::from_utf8(output.stdout)
+            .map_err(|e| anyhow!("Invalid UTF-8 from hhd.steamos steamos-tdp get: {e}"))?;
+
+        let mut parts = stdout.split_whitespace();
+        let min: u32 = parts
+            .next()
+            .ok_or_else(|| anyhow!("Missing min value"))?
+            .parse()
+            .map_err(|e| anyhow!("Failed to parse min value: {e}"))?;
+        let max: u32 = parts
+            .next()
+            .ok_or_else(|| anyhow!("Missing max value"))?
+            .parse()
+            .map_err(|e| anyhow!("Failed to parse max value: {e}"))?;
+        Ok(min..=max)
+    }
+
+    async fn is_active(&self) -> Result<bool> {
+        Ok(true)
+    }
+}
+
 async fn find_battery_charge_path() -> Result<PathBuf> {
     let config = device_config().await?;
     let config = config
@@ -805,15 +913,19 @@ impl TdpManagerService {
         system: &Connection,
         session: &Connection,
     ) -> Result<TdpManagerService> {
-        let config = device_config().await?;
-        let download_mode_limit = config
-            .as_ref()
-            .and_then(|config| config.tdp_limit.as_ref())
-            .and_then(|config| config.download_mode_limit);
-        let download_mode_fan_speed = config
-            .as_ref()
-            .and_then(|config| config.fan_speed.as_ref())
-            .and_then(|config| config.download_mode_fan_speed);
+        let (download_mode_limit, download_mode_fan_speed) = match device_config().await {
+            Ok(Some(config)) => (
+                config
+                    .tdp_limit
+                    .as_ref()
+                    .and_then(|tdp| tdp.download_mode_limit),
+                config
+                    .fan_speed
+                    .as_ref()
+                    .and_then(|fan| fan.download_mode_fan_speed),
+            ),
+            _ => (None, None),
+        };
 
         let manager = tdp_limit_manager(system).await?;
         let proxy = RootManagerProxy::new(system).await?;

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 22:34:27 +0200
Subject: fix: add targets to ensure this starts before hhd and steam

Signed-off-by: Antheas Kapenekakis <git@antheas.dev>
---
 data/system/steamos-manager.service | 4 ++++
 data/user/steamos-manager.service   | 2 ++
 2 files changed, 6 insertions(+)

diff --git a/data/system/steamos-manager.service b/data/system/steamos-manager.service
index e49ea157d6a3..08f77ccab1e0 100644
--- a/data/system/steamos-manager.service
+++ b/data/system/steamos-manager.service
@@ -2,6 +2,10 @@
 Description=SteamOS Manager Daemon
 Wants=steamos-log-submitter.service
 After=steamos-log-submitter.service
+Wants=hhd.service
+Wants=hhdl.service
+After=hhd.service
+After=hhdl.service
 StartLimitIntervalSec=120
 StartLimitBurst=5
 
diff --git a/data/user/steamos-manager.service b/data/user/steamos-manager.service
index c6dc9febe6c9..670e643df7df 100644
--- a/data/user/steamos-manager.service
+++ b/data/user/steamos-manager.service
@@ -1,5 +1,7 @@
 [Unit]
 Description=SteamOS Manager Daemon
+After=hhd.service
+After=hhdl.service
 StartLimitIntervalSec=120
 StartLimitBurst=5
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Tue, 16 Jun 2026 00:17:16 +0200
Subject: fix: align HDMI CEC setters with zbus interface

---
 steamos-manager/src/manager/user.rs | 19 +++++++++----------
 1 file changed, 9 insertions(+), 10 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index 4f1f4b83466c..dec2233c3eb3 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -627,13 +627,13 @@ impl HdmiCec2 {
     }
 
     #[zbus(property)]
-    async fn set_enable_control(&self, enable: bool) -> fdo::Result<()> {
+    async fn set_enable_control(&self, enable: bool) -> zbus::Result<()> {
         self.hdmi_cec
             .lock()
             .await
             .set_enable_control(enable)
             .await
-            .map_err(to_zbus_fdo_error)
+            .map_err(to_zbus_error)
     }
 
     #[zbus(property)]
@@ -647,13 +647,13 @@ impl HdmiCec2 {
     }
 
     #[zbus(property)]
-    async fn set_suspend_tv(&self, enable: bool) -> fdo::Result<()> {
+    async fn set_suspend_tv(&self, enable: bool) -> zbus::Result<()> {
         self.hdmi_cec
             .lock()
             .await
             .set_suspend_tv(enable)
             .await
-            .map_err(to_zbus_fdo_error)
+            .map_err(to_zbus_error)
     }
 
     #[zbus(property)]
@@ -667,13 +667,13 @@ impl HdmiCec2 {
     }
 
     #[zbus(property)]
-    async fn set_suspend_device(&self, enable: bool) -> fdo::Result<()> {
+    async fn set_suspend_device(&self, enable: bool) -> zbus::Result<()> {
         self.hdmi_cec
             .lock()
             .await
             .set_suspend_device(enable)
             .await
-            .map_err(to_zbus_fdo_error)
+            .map_err(to_zbus_error)
     }
 
     #[zbus(property)]
@@ -687,13 +687,13 @@ impl HdmiCec2 {
     }
 
     #[zbus(property)]
-    async fn set_wake_tv(&self, enable: bool) -> fdo::Result<()> {
+    async fn set_wake_tv(&self, enable: bool) -> zbus::Result<()> {
         self.hdmi_cec
             .lock()
             .await
             .set_wake_tv(enable)
             .await
-            .map_err(to_zbus_fdo_error)
+            .map_err(to_zbus_error)
     }
 
     #[zbus(property)]
@@ -714,11 +714,10 @@ impl HdmiCec2 {
     }
 
     #[zbus(property)]
-    async fn set_wake_device(&self, enable: bool) -> fdo::Result<()> {
+    async fn set_wake_device(&self, enable: bool) -> zbus::Result<()> {
         self.manager
             .set_hdmi_cec_awaken(enable)
             .await
-            .map_err(to_zbus_fdo_error)
     }
 
     #[zbus(property(emits_changed_signal = "const"))]

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Sat, 11 Jul 2026 02:45:59 +0200
Subject: fix: use Fedora libdir for steamos-manager services

---
 data/system/steamos-manager.service | 2 +-
 data/user/steamos-manager.service   | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/data/system/steamos-manager.service b/data/system/steamos-manager.service
index 08f77ccab1e0..5336a76b1442 100644
--- a/data/system/steamos-manager.service
+++ b/data/system/steamos-manager.service
@@ -13,7 +13,7 @@ StartLimitBurst=5
 Type=notify-reload
 BusName=com.steampowered.SteamOSManager1
 Environment=RUST_LOG=info
-ExecStart=/usr/lib/steamos-manager -r
+ExecStart=/usr/lib64/steamos-manager -r
 Restart=on-failure
 RestartSec=1
 RestartMaxDelaySec=10
diff --git a/data/user/steamos-manager.service b/data/user/steamos-manager.service
index 670e643df7df..2c6fdb868701 100644
--- a/data/user/steamos-manager.service
+++ b/data/user/steamos-manager.service
@@ -9,7 +9,7 @@ StartLimitBurst=5
 Type=notify-reload
 BusName=com.steampowered.SteamOSManager1
 Environment=RUST_LOG=info
-ExecStart=/usr/lib/steamos-manager
+ExecStart=/usr/lib64/steamos-manager
 Restart=on-failure
 RestartSec=1
 RestartMaxDelaySec=10

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:15:06 +0200
Subject: remove inputplumber handling

---
 steamos-manager/src/daemon/root.rs | 4 ----
 steamos-manager/src/lib.rs         | 1 -
 2 files changed, 5 deletions(-)

diff --git a/steamos-manager/src/daemon/root.rs b/steamos-manager/src/daemon/root.rs
index 7a7121533fec..69bf42ffff01 100644
--- a/steamos-manager/src/daemon/root.rs
+++ b/steamos-manager/src/daemon/root.rs
@@ -19,7 +19,6 @@ use zbus::connection::Connection;
 
 use crate::daemon::{Daemon, DaemonCommand, DaemonContext, channel};
 use crate::ds_inhibit::Inhibitor;
-use crate::inputplumber::DeckService;
 use crate::manager::root::SteamOSManager;
 use crate::path;
 use crate::sls::ftrace::Ftrace;
@@ -127,9 +126,6 @@ impl DaemonContext for RootContext {
         let ftrace = Ftrace::init(&connection).await?;
         daemon.add_service(ftrace);
 
-        let ip = DeckService::init(connection);
-        daemon.add_service(ip);
-
         let sysfs = SysfsWriterService::init()?;
         daemon.add_service(sysfs);
 
diff --git a/steamos-manager/src/lib.rs b/steamos-manager/src/lib.rs
index 17e607bff42d..ed2908f974d1 100644
--- a/steamos-manager/src/lib.rs
+++ b/steamos-manager/src/lib.rs
@@ -27,7 +27,6 @@ pub use steamos_manager_proxy as proxy;
 
 mod ds_inhibit;
 mod error;
-mod inputplumber;
 mod job;
 mod manager;
 mod platform;

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:22:42 +0200
Subject: drop wifi interfaces

---
 steamos-manager/src/manager/user.rs | 42 +++++++++--------------------
 1 file changed, 12 insertions(+), 30 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index dec2233c3eb3..ddfec3107186 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -1876,7 +1876,7 @@ pub(crate) async fn create_interfaces(
     if device_type().await.unwrap_or_default() == "steam_deck" {
         object_server.at(MANAGER_PATH, als).await?;
     }
-    object_server.at(MANAGER_PATH, wifi_backend).await?;
+    drop(wifi_backend);
     if steam_deck_variant().await.unwrap_or_default() == SteamDeckVariant::Galileo {
         let wifi_debug = WifiDebug1 {
             proxy: proxy.clone(),
@@ -1884,8 +1884,8 @@ pub(crate) async fn create_interfaces(
         let wifi_debug_dump = WifiDebugDump1 {
             proxy: proxy.clone(),
         };
-        object_server.at(MANAGER_PATH, wifi_debug).await?;
-        object_server.at(MANAGER_PATH, wifi_debug_dump).await?;
+        drop(wifi_debug);
+        drop(wifi_debug_dump);
     }
 
     if power_features {
@@ -2005,9 +2005,7 @@ pub(crate) async fn create_interfaces(
     }
 
     if !list_wifi_interfaces().await.unwrap_or_default().is_empty() {
-        object_server
-            .at(MANAGER_PATH, wifi_power_management)
-            .await?;
+        drop(wifi_power_management);
     }
 
     remote_interface.context_tdp_limit1 = tdp_manager;
@@ -2883,25 +2881,17 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_wifi_backend1() {
+    async fn interface_missing_wifi_backend1() {
         let test = start(TestConfig::all()).await.expect("start");
 
-        assert!(
-            test_interface_matches::<WifiBackend1>(&test.connection)
-                .await
-                .unwrap()
-        );
+        assert!(test_interface_missing::<WifiBackend1>(&test.connection).await);
     }
 
     #[tokio::test]
-    async fn interface_matches_wifi_debug1() {
+    async fn interface_missing_wifi_debug1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
-        assert!(
-            test_interface_matches::<WifiDebug1>(&test.connection)
-                .await
-                .unwrap()
-        );
+        assert!(test_interface_missing::<WifiDebug1>(&test.connection).await);
     }
 
     #[tokio::test]
@@ -2912,14 +2902,10 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_wifi_debug_dump1() {
+    async fn interface_missing_wifi_debug_dump1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
-        assert!(
-            test_interface_matches::<WifiDebugDump1>(&test.connection)
-                .await
-                .unwrap()
-        );
+        assert!(test_interface_missing::<WifiDebugDump1>(&test.connection).await);
     }
 
     #[tokio::test]
@@ -2930,14 +2916,10 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_wifi_power_management1() {
+    async fn interface_missing_wifi_power_management1() {
         let test = start(TestConfig::all()).await.expect("start");
 
-        assert!(
-            test_interface_matches::<WifiPowerManagement1>(&test.connection)
-                .await
-                .unwrap()
-        );
+        assert!(test_interface_missing::<WifiPowerManagement1>(&test.connection).await);
     }
 
     async fn register_remote<I: RemoteInterface + Interface>(

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:24:37 +0200
Subject: drop session management

---
 steamos-manager/src/manager/user.rs | 10 +++-------
 1 file changed, 3 insertions(+), 7 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index ddfec3107186..2d4229859fde 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -2001,7 +2001,7 @@ pub(crate) async fn create_interfaces(
                 }
             },
         }
-        object_server.at(MANAGER_PATH, session_management).await?;
+        drop(session_management);
     }
 
     if !list_wifi_interfaces().await.unwrap_or_default().is_empty() {
@@ -2693,16 +2693,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_session_management1() {
+    async fn interface_missing_session_management1_with_managed_setup() {
         let test = start(TestConfig::only_setup(ManagedSetup {}))
             .await
             .expect("start");
 
-        assert!(
-            test_interface_matches::<SessionManagement1>(&test.connection)
-                .await
-                .unwrap()
-        );
+        assert!(test_interface_missing::<SessionManagement1>(&test.connection).await);
     }
 
     #[tokio::test]

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:30:19 +0200
Subject: drop storage management and reset

---
 steamos-manager/src/manager/user.rs | 20 ++++++--------------
 1 file changed, 6 insertions(+), 14 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index 2d4229859fde..293d2808200c 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -208,8 +208,6 @@ struct RemoteInterface1 {
     #[remote]
     remote_cpu_boost1: Option<CpuBoost1RemoteOwner>,
     #[remote]
-    remote_factory_reset1: Option<FactoryReset1RemoteOwner>,
-    #[remote]
     remote_fan_control1: Option<FanControl1RemoteOwner>,
     #[remote]
     remote_gpu_performance_level1: Option<GpuPerformanceLevel1RemoteOwner>,
@@ -872,7 +870,6 @@ impl RemoteInterface1 {
             job_manager,
             remote_battery_charge_limit1: None,
             remote_cpu_boost1: None,
-            remote_factory_reset1: None,
             remote_fan_control1: None,
             remote_gpu_performance_level1: None,
             remote_gpu_power_profile1: None,
@@ -1679,7 +1676,7 @@ async fn create_platform_interfaces(
     if let Some(config) = config.factory_reset.as_ref() {
         match config.is_valid(true).await {
             Ok(true) => {
-                object_server.at(MANAGER_PATH, factory_reset).await?;
+                drop(factory_reset);
             }
             Ok(false) => (),
             Err(e) => error!("Failed to verify if factory reset config is valid: {e}"),
@@ -1699,7 +1696,7 @@ async fn create_platform_interfaces(
     if let Some(config) = config.storage.as_ref() {
         match config.is_valid(true).await {
             Ok(true) => {
-                object_server.at(MANAGER_PATH, storage).await?;
+                drop(storage);
             }
             Ok(false) => (),
             Err(e) => error!("Failed to verify if storage config is valid: {e}"),
@@ -2429,14 +2426,10 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_factory_reset1() {
+    async fn interface_missing_factory_reset1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
-        assert!(
-            test_interface_matches::<FactoryReset1>(&test.connection)
-                .await
-                .unwrap()
-        );
+        assert!(test_interface_missing::<FactoryReset1>(&test.connection).await);
     }
 
     #[tokio::test]
@@ -2709,13 +2702,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_storage1() {
+    async fn interface_missing_storage1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<Storage1>(&test.connection)
+            test_interface_missing::<Storage1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:39:14 +0200
Subject: drop other hardware methods

---
 steamos-manager/src/manager/user.rs | 35 +++++++++++++----------------
 1 file changed, 15 insertions(+), 20 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index 293d2808200c..8843f0d5b0d5 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -1686,7 +1686,7 @@ async fn create_platform_interfaces(
     if let Some(config) = config.fan_control.as_ref() {
         match config.is_valid(connection, true).await {
             Ok(true) => {
-                object_server.at(MANAGER_PATH, fan_control).await?;
+                drop(fan_control);
             }
             Ok(false) => (),
             Err(e) => error!("Failed to verify if fan control config is valid: {e}"),
@@ -1887,16 +1887,16 @@ pub(crate) async fn create_interfaces(
 
     if power_features {
         if get_max_charge_level().await.is_ok() {
-            object_server.at(MANAGER_PATH, battery_charge_limit).await?;
+            drop(battery_charge_limit);
         }
 
         if get_cpu_boost_state().await.is_ok() {
-            object_server.at(MANAGER_PATH, cpu_boost).await?;
+            drop(cpu_boost);
         }
 
-        object_server.at(MANAGER_PATH, cpu_scaling).await?;
+        drop(cpu_scaling);
         if CpuSchedulerManager::is_supported().await? {
-            object_server.at(MANAGER_PATH, cpu_scheduler).await?;
+            drop(cpu_scheduler);
         }
 
         match gpu_power_profile_driver().await {
@@ -2361,13 +2361,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_battery_charge_limit() {
+    async fn interface_missing_battery_charge_limit_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<BatteryChargeLimit1>(&test.connection)
+            test_interface_missing::<BatteryChargeLimit1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 
@@ -2379,13 +2378,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_cpu_boost1() {
+    async fn interface_missing_cpu_boost1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<CpuBoost1>(&test.connection)
+            test_interface_missing::<CpuBoost1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 
@@ -2397,24 +2395,22 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_cpu_scaling1() {
+    async fn interface_missing_cpu_scaling1() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<CpuScaling1>(&test.connection)
+            test_interface_missing::<CpuScaling1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 
     #[tokio::test]
-    async fn interface_matches_cpu_scheduler1() {
+    async fn interface_missing_cpu_scheduler1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<CpuScheduler1>(&test.connection)
+            test_interface_missing::<CpuScheduler1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 
@@ -2494,13 +2490,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_fan_control1() {
+    async fn interface_missing_fan_control1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<FanControl1>(&test.connection)
+            test_interface_missing::<FanControl1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:43:21 +0200
Subject: drop cec handling

---
 steamos-manager/src/manager/user.rs | 34 ++++++++++-------------------
 1 file changed, 11 insertions(+), 23 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index 8843f0d5b0d5..a3c2fbeae2be 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -1950,23 +1950,13 @@ pub(crate) async fn create_interfaces(
 
     if let Ok(hdmi_cec) = hdmi_cec {
         let hdmi_cec = Arc::new(Mutex::new(hdmi_cec));
-        object_server
-            .at(
-                MANAGER_PATH,
-                HdmiCec1 {
-                    hdmi_cec: hdmi_cec.clone(),
-                },
-            )
-            .await?;
-        object_server
-            .at(
-                MANAGER_PATH,
-                HdmiCec2 {
-                    hdmi_cec,
-                    manager: root_manager,
-                },
-            )
-            .await?;
+        drop(HdmiCec1 {
+            hdmi_cec: hdmi_cec.clone(),
+        });
+        drop(HdmiCec2 {
+            hdmi_cec,
+            manager: root_manager,
+        });
     }
 
     object_server.at(MANAGER_PATH, manager2).await?;
@@ -2543,15 +2533,14 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_hdmi_cec1() {
+    async fn interface_missing_hdmi_cec1_with_cecd() {
         let test = start(TestConfig::only_setup(CecdSetup {}))
             .await
             .expect("start");
 
         assert!(
-            test_interface_matches::<HdmiCec1>(&test.connection)
+            test_interface_missing::<HdmiCec1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 
@@ -2563,15 +2552,14 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_hdmi_cec2() {
+    async fn interface_missing_hdmi_cec2_with_cecd() {
         let test = start(TestConfig::only_setup(CecdSetup {}))
             .await
             .expect("start");
 
         assert!(
-            test_interface_matches::<HdmiCec2>(&test.connection)
+            test_interface_missing::<HdmiCec2>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 23:44:51 +0200
Subject: drop firmware update methods

---
 steamos-manager/src/manager/user.rs | 16 +++++++---------
 1 file changed, 7 insertions(+), 9 deletions(-)

diff --git a/steamos-manager/src/manager/user.rs b/steamos-manager/src/manager/user.rs
index a3c2fbeae2be..96a4e0ea5875 100644
--- a/steamos-manager/src/manager/user.rs
+++ b/steamos-manager/src/manager/user.rs
@@ -1646,7 +1646,7 @@ impl Service for ScreenReaderSetupService {
 
 async fn create_platform_interfaces(
     proxy: &Proxy<'static>,
-    object_server: &ObjectServer,
+    _object_server: &ObjectServer,
     connection: &Connection,
     job_manager: &UnboundedSender<JobManagerCommand>,
 ) -> Result<()> {
@@ -1706,7 +1706,7 @@ async fn create_platform_interfaces(
     if let Some(config) = config.update_bios.as_ref() {
         match config.is_valid(true).await {
             Ok(true) => {
-                object_server.at(MANAGER_PATH, update_bios).await?;
+                drop(update_bios);
             }
             Ok(false) => (),
             Err(e) => error!("Failed to verify if BIOS update config is valid: {e}"),
@@ -1716,7 +1716,7 @@ async fn create_platform_interfaces(
     if let Some(config) = config.update_dock.as_ref() {
         match config.is_valid(true).await {
             Ok(true) => {
-                object_server.at(MANAGER_PATH, update_dock).await?;
+                drop(update_dock);
             }
             Ok(false) => (),
             Err(e) => error!("Failed to verify if dock update config is valid: {e}"),
@@ -2780,13 +2780,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_update_bios1() {
+    async fn interface_missing_update_bios1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<UpdateBios1>(&test.connection)
+            test_interface_missing::<UpdateBios1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 
@@ -2816,13 +2815,12 @@ mod test {
     }
 
     #[tokio::test]
-    async fn interface_matches_update_dock1() {
+    async fn interface_missing_update_dock1_with_all_config() {
         let test = start(TestConfig::all()).await.expect("start");
 
         assert!(
-            test_interface_matches::<UpdateDock1>(&test.connection)
+            test_interface_missing::<UpdateDock1>(&test.connection)
                 .await
-                .unwrap()
         );
     }
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 20 Jul 2026 13:16:58 +0200
Subject: drop ftrace, it also does not have a selinux policy

---
 data/system/steamos-manager.service | 2 --
 steamos-manager/src/daemon/root.rs  | 5 -----
 steamos-manager/src/sls/mod.rs      | 2 --
 3 files changed, 9 deletions(-)

diff --git a/data/system/steamos-manager.service b/data/system/steamos-manager.service
index 5336a76b1442..6e97c829cdc0 100644
--- a/data/system/steamos-manager.service
+++ b/data/system/steamos-manager.service
@@ -1,7 +1,5 @@
 [Unit]
 Description=SteamOS Manager Daemon
-Wants=steamos-log-submitter.service
-After=steamos-log-submitter.service
 Wants=hhd.service
 Wants=hhdl.service
 After=hhd.service
diff --git a/steamos-manager/src/daemon/root.rs b/steamos-manager/src/daemon/root.rs
index 69bf42ffff01..87d6710bdaec 100644
--- a/steamos-manager/src/daemon/root.rs
+++ b/steamos-manager/src/daemon/root.rs
@@ -21,7 +21,6 @@ use crate::daemon::{Daemon, DaemonCommand, DaemonContext, channel};
 use crate::ds_inhibit::Inhibitor;
 use crate::manager::root::SteamOSManager;
 use crate::path;
-use crate::sls::ftrace::Ftrace;
 use crate::sls::{LogLayer, LogReceiver};
 use crate::sysfs::SysfsWriterService;
 
@@ -122,10 +121,6 @@ impl DaemonContext for RootContext {
     ) -> Result<()> {
         self.state = state;
 
-        let connection = daemon.get_connection();
-        let ftrace = Ftrace::init(&connection).await?;
-        daemon.add_service(ftrace);
-
         let sysfs = SysfsWriterService::init()?;
         daemon.add_service(sysfs);
 
diff --git a/steamos-manager/src/sls/mod.rs b/steamos-manager/src/sls/mod.rs
index 2ecba2a92997..f2da031b2ec4 100644
--- a/steamos-manager/src/sls/mod.rs
+++ b/steamos-manager/src/sls/mod.rs
@@ -1,6 +1,4 @@
 /* SPDX-License-Identifier: BSD-2-Clause */
-pub mod ftrace;
-
 use anyhow::Result;
 use std::fmt::Debug;
 use std::time::SystemTime;

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Tue, 25 Aug 2026 19:29:31 +0200
Subject: ds_inhibit: Wait for controller input enumeration

---
 steamos-manager/src/ds_inhibit.rs | 93 ++++++++++++++++++++++++++++---
 1 file changed, 85 insertions(+), 8 deletions(-)

diff --git a/steamos-manager/src/ds_inhibit.rs b/steamos-manager/src/ds_inhibit.rs
index db897ce51c3a..b3c2bcd09e3b 100644
--- a/steamos-manager/src/ds_inhibit.rs
+++ b/steamos-manager/src/ds_inhibit.rs
@@ -23,6 +23,12 @@ struct HidNode {
     id: u32,
 }
 
+enum InhibitStatus {
+    Ready,
+    Pending,
+    Unsupported,
+}
+
 pub struct Inhibitor {
     inotify: EventStream<[u8; 512]>,
     dev_watch: WatchDescriptor,
@@ -63,16 +69,20 @@ impl HidNode {
         Ok(entries)
     }
 
-    async fn can_inhibit(&self) -> bool {
+    async fn inhibit_status(&self) -> InhibitStatus {
         debug!("Checking if hidraw{} can be inhibited", self.id);
         let driver = match read_link(self.sys_base().join("driver")).await {
             Ok(driver) => driver,
+            Err(e) if e.kind() == ErrorKind::NotFound => {
+                debug!("Driver for hidraw{} has not enumerated yet", self.id);
+                return InhibitStatus::Pending;
+            }
             Err(e) => {
                 warn!(
                     "Failed to find associated driver for hidraw{}: {}",
                     self.id, e
                 );
-                return false;
+                return InhibitStatus::Unsupported;
             }
         };
 
@@ -81,20 +91,46 @@ impl HidNode {
             Some("sony" | "playstation")
         ) {
             debug!("Not a PlayStation controller");
-            return false;
+            return InhibitStatus::Unsupported;
         }
         let nodes = match self.get_nodes().await {
             Ok(nodes) => nodes,
+            Err(e) if e.kind() == ErrorKind::NotFound => {
+                debug!("Inputs for hidraw{} have not enumerated yet", self.id);
+                return InhibitStatus::Pending;
+            }
             Err(e) => {
                 warn!("Failed to list inputs for hidraw{}: {e}", self.id);
-                return false;
+                return InhibitStatus::Unsupported;
             }
         };
         if nodes.is_empty() {
-            debug!("No nodes to inhibit");
-            return false;
+            debug!("No nodes to inhibit yet");
+            return InhibitStatus::Pending;
+        }
+        InhibitStatus::Ready
+    }
+
+    #[cfg(test)]
+    async fn can_inhibit(&self) -> bool {
+        matches!(self.inhibit_status().await, InhibitStatus::Ready)
+    }
+
+    async fn wait_until_inhibitable(&self) -> bool {
+        const RETRY_DELAY: Duration = Duration::from_millis(250);
+        const RETRIES: usize = 20;
+
+        for retry in 0..=RETRIES {
+            match self.inhibit_status().await {
+                InhibitStatus::Ready => return true,
+                InhibitStatus::Unsupported => return false,
+                InhibitStatus::Pending if retry < RETRIES => sleep(RETRY_DELAY).await,
+                InhibitStatus::Pending => break,
+            }
         }
-        true
+
+        debug!("Timed out waiting for hidraw{} to enumerate", self.id);
+        false
     }
 
     async fn check(&self) -> Result<()> {
@@ -218,7 +254,7 @@ impl Inhibitor {
         };
 
         let node = HidNode::new(id);
-        if !node.can_inhibit().await {
+        if !node.wait_until_inhibitable().await {
             return Ok(false);
         }
         info!("Adding {} to watchlist", path.display());
@@ -575,6 +611,47 @@ mod test {
         );
     }
 
+    #[tokio::test]
+    async fn inhibitor_delayed_input_enumeration() {
+        let h = testing::start();
+        let path = h.test.path();
+
+        let hid = HidNode::new(0);
+        let sys_base = hid.sys_base();
+
+        create_dir_all(path.join("dev")).await.expect("dev");
+        write(hid.hidraw(), "").await.expect("hidraw");
+        create_dir_all(&sys_base).await.expect("sysfs device");
+        symlink("playstation", sys_base.join("driver"))
+            .await
+            .expect("driver");
+        create_dir_all(path.join("proc/1/fd")).await.expect("fd");
+        symlink(hid.hidraw(), path.join("proc/1/fd/3"))
+            .await
+            .expect("symlink");
+        write(path.join("proc/1/comm"), "steam\n")
+            .await
+            .expect("comm");
+
+        let input = sys_base.join("input/input0/mouse0");
+        let enumerate = tokio::spawn(async move {
+            sleep(Duration::from_millis(50)).await;
+            create_dir_all(input).await.expect("mouse0");
+        });
+
+        let mut inhibitor = Inhibitor::init().await.expect("init");
+        enumerate.await.expect("enumerate");
+
+        assert_eq!(
+            read_to_string(sys_base.join("input/input0/inhibited"))
+                .await
+                .expect("inhibited"),
+            "1\n"
+        );
+
+        inhibitor.shutdown().await.expect("stop");
+    }
+
     #[tokio::test]
     async fn inhibitor_open_close() {
         let h = testing::start();
